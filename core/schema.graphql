type Query {
    hello(name: String): String!
    post(id: ID!): Post
    posts: [Post]
}

type Post {
    id: ID!
    title: String!
    text: String!
    metadesc: String
    user: User
    created_at: String!
    updated_at: String!
}

type User {
    id: ID!
    name: String!
}